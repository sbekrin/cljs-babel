{
  "name": "cljs-compiler",
  "version": "1.0.0",
  "description": "ClojureScript to JavaScript compiler without binary dependencies",
  "main": "lib/index.js",
  "scripts": {
    "pretest": "$CI && npm link && npm link cljs-compiler",
    "test": "ava"
  },
  "author": "Sergey Bekrin <sergey@bekrin.me> (http://bekrin.me)",
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.18.2",
    "common-tags": "^1.4.0"
  },
  "engines": {
    "node": ">=4.9.x"
  }
}
